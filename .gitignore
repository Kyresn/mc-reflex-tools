# Gradle
.gradle/
build/
**/build/
.kotlin/

# IDE
.idea/
.vscode/
bin/
*.iml
*.ipr
*.iws
.settings/
.classpath
.project

# Local agent/editor state
.remember/
.claude/

# Minecraft development
run/
runs/
logs/
crash-reports/

# Native builds
nvidia-sdk-native/build/
nvidia-sdk-native/out/
*.dll
*.lib
*.pdb
*.so
*.dylib

# ...except the native bridge that is embedded in the mod jar. It is a build
# input, not a build output, so a fresh clone must contain it.
!nvidia-sdk-java/src/main/resources/natives/windows-x64/*.dll

# Streamline plugin logs written next to the Minecraft run directory
mc_reflex_tools_logs/

# Java
*.class
hs_err_*.log
replay_*.log
*.hprof
*.jfr

# OS
.DS_Store
Thumbs.db
